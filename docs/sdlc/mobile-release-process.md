# Mobile Release Process

## Branching

- Use short-lived feature branches.
- Require CI on pull requests.
- Release branches are optional until native builds begin.

## Candidate Creation

1. Update requirements, changelog, and versioning notes.
2. Run `npm run release:check`.
3. Run `npm run release:env:doctor` and confirm `.env.example` lists runtime, smoke, and release key names without values.
4. Run `npm run release:readiness` and review `docs/sdlc/launch-readiness-report.json`.
5. Review the Plan tab release unblock checklist for remaining proof artifacts, commands, and credential key names.
6. Run `npm run release:eas:check`.
7. Run `npm run native:qa:runbook` before collecting physical-device evidence.
8. Run browser smoke.
9. For native releases, run EAS preview build.
10. Run `npm run release:archives` after web export to refresh delivery zips and checksum manifests.
11. Run `npm run release:handoff` after screenshots, readiness reports, and archives are refreshed.
12. Complete privacy and permission review.

## EAS Readiness

The standard EAS gate is safe for local and CI use because it verifies release configuration and reports account-bound
requirements as warnings:

```bash
npm run release:eas:check
```

The strict EAS gate must pass before any store submission:

```bash
npm run release:eas:strict
```

Strict mode requires:

- `extra.eas.projectId` in `app.json`, generated by `npx eas-cli@latest init` on the target Expo account.
- `EXPO_TOKEN` for non-interactive EAS builds.
- `MOVEBETA_ASC_APP_ID` plus either `MOVEBETA_APPLE_ID` or `ASC_API_KEY_ID`, `ASC_API_ISSUER_ID`,
  and `ASC_API_KEY_P8_BASE64` for iOS submission.
- One of `GOOGLE_SERVICE_ACCOUNT_JSON`, `GOOGLE_SERVICE_ACCOUNT_KEY_PATH`, or
  `MOVEBETA_GOOGLE_SERVICE_ACCOUNT_JSON_BASE64` for Play submission.

Credential values must stay in local shell secrets, CI secrets, or EAS credentials. Do not commit store credential files or
secret-like values into `eas.json`.

`.env.example` is the share-safe setup contract. Regenerate `docs/sdlc/env-template-report.json` with
`npm run release:env:doctor` after adding runtime, smoke-test, or release credential key names.

The Plan tab release unblock checklist mirrors these strict prerequisites. It lists the required proof artifacts,
commands, affected release tracks, owners, and credential key names while deliberately excluding credential values.
Use the Plan tab evidence reconciliation panel before flipping launch evidence flags: paste only share-safe generated
reports, confirm which blockers would clear, and reject any JSON that contains raw video paths, credential values, or
token-like strings.

## Go/No-Go

Go only when:

- CI passes.
- `npm run release:eas:strict` passes for store-bound builds.
- No unaccepted P0/P1 risks remain.
- Native permission copy matches actual behavior.
- Raw video upload remains opt-in.
- Rollback path is known.

## Store Submission

Store submission is prepared by the store readiness kit, but production submission is still gated by native device QA.
Before submitting:

- Regenerate `docs/store/store-manifest.json` with `npm run store:manifest`.
- Regenerate `docs/store/store-submission-packet.json` with `npm run store:submission`.
- Capture current screenshots with `npm run store:screenshots`.
- Refresh `docs/sdlc/launch-readiness-report.json` with `npm run release:readiness`.
- Refresh `docs/sdlc/native-qa-runbook.json` with `npm run native:qa:runbook`.
- Replace placeholder bundle/package identifiers if needed.
- Confirm Apple/Google account ownership.
- Prepare privacy labels and data safety forms.
- Attach support and deletion contact flow.
- Verify the store listing does not claim medical, injury-prevention, safety, or grade-prediction outcomes.

Recommended store build commands after strict readiness passes:

```bash
npx eas-cli@latest build -p ios --profile production
npx eas-cli@latest build -p android --profile production
npx eas-cli@latest build -p ios --profile production --submit
npx eas-cli@latest build -p android --profile production --submit
```

## Handoff Packet

Generate the current release archives and handoff packet before sharing the project with buyers, stakeholders, or external QA:

```bash
npm run release:archives
npm run release:handoff
```

`npm run release:archives` writes `../movebeta-mobile-source.zip`, `../movebeta-mobile-web-dist.zip`,
`../movebeta-mobile-release-archives.json`, and `../movebeta-mobile-release-archives.md` with byte sizes and SHA-256
checksums plus repository and worktree-state evidence.

The command writes `docs/sdlc/release-handoff-packet.json` and `docs/sdlc/release-handoff-packet.md` from the current
machine-readable reports, store screenshot manifest, repository commit, and launch blockers.
