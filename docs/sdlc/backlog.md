# Product Backlog

| ID | Epic | User story | Priority | Acceptance notes |
| --- | --- | --- | --- | --- |
| MB-001 | Native inference | As a climber, I can analyze an imported clip on my phone without cloud upload | Done | Custom native provider returns pose frames through the MoveBetaPose bridge, with Apple Vision on iOS, ML Kit on Android, and static bridge contract tests |
| MB-002 | Local storage | As a climber, I can keep and delete local reports | Done | SQLite native repository plus web/local fallback |
| MB-003 | Real clip validation | As a product team, we can measure cue usefulness on consented clips | Must | Dataset contract, template, CLI gate, scoring harness, and rubric ready; needs real consented clip set |
| MB-003A | Validation study seed | As a product team, I can prepare a privacy-safe study seed from consented local packets | Done | Sessions exports packet-only review tasks from active cue-validation consent without raw artifacts or invented reviewer scores |
| MB-003A1 | Validation clip intake manifest | As a product team, I can verify consented clip coverage before sending worksheets to coaches | Done | Sessions prepares a versioned clip manifest with coverage, wall-angle gaps, review-row counts, negative privacy flags, and no coach packet payloads or raw artifacts |
| MB-003B | Validation worksheet | As a product team, I can give reviewers a blank worksheet for real cue scores | Done | Sessions exports null reviewer identities and null score fields for each packet-only task without raw artifacts |
| MB-003C | Validation CSV | As a product team, I can export the review worksheet to a spreadsheet-friendly CSV | Done | Sessions exports stable CSV headers with blank reviewer/score cells, escaped values, and raw-artifact text rejection |
| MB-003D | Completed validation dataset | As a product team, I can turn completed worksheet CSV into validation dataset JSON | Done | Domain composer accepts only real reviewer ids, 1-5 scores, expected rows, and seed-matching metadata before producing versioned gate-compatible JSON |
| MB-003E | Validation dataset UI | As a product team, I can build the validation dataset from completed CSV inside Sessions | Done | Sessions accepts completed worksheet CSV, reports validation errors, and prepares versioned gate-compatible JSON locally |
| MB-003F | Validation gate preview | As a product team, I can see whether a completed validation dataset is production-ready before sharing it | Done | Sessions runs a local gate preview and reports ready or needs-data checks without cloud calls |
| MB-003G | Validation gate parity | As a maintainer, I can trust that the in-app validation preview matches the release CLI gate | Done | Automated parity tests compare ready, evidence-gap, and raw-artifact rejection outcomes across the app and CLI gates |
| MB-004 | Repeat comparison | As a climber, I can compare two attempts of the same move | Done | Latest-vs-baseline metrics, cue status, and recommendation in Progress |
| MB-005 | Pro history | As a paying user, I can track long-term technique trends | Done | Active plan entitlements gate recent vs unlimited history; optional sync future |
| MB-005A | Adaptive drills | As a climber, my drill plan reacts to which cues actually helped | Done | Private cue feedback marks drills as reinforce, variant, or untested without cloud sync |
| MB-005B | Drill practice log | As a climber, I can record whether I completed or skipped suggested drills | Done | Private local records are persisted, backed up, restored, and deleted with the related report |
| MB-005C | Practice consistency | As a climber, I can see whether I am following through on suggested drills | Done | Progress summarizes completion rate, latest drill status, skipped cue review, and next practice recommendation |
| MB-005D | Practice-aware planning | As a climber, my next session adapts when I keep skipping drills | Done | Session plan lowers intensity and prescribes an easier reset variant when skipped practice exceeds completions |
| MB-005E | Beta replay plan | As a climber, I can turn a video analysis into a concrete repeat sequence | Done | Analyze shows setup, crux, and exit actions from local cue and metric evidence before the next attempt |
| MB-005F | Movement phase breakdown | As a climber, I can see whether launch, crux, or finish needs the reset | Done | Analyze scores launch, crux, and finish from local cues and timeline events and highlights the primary phase |
| MB-005G | Cue trust | As a climber, I can see how much to trust each cue before changing beta | Done | Analyze and coach packet exports score each cue from pose quality, timing evidence, runtime budget, and validation readiness |
| MB-005H | Repeat outcome loop | As a climber, I can log whether the corrected repeat improved, sent, fell, or regressed | Done | Sessions stores repeat status, attempts, and resolved cues; Progress summarizes success, stalled repeats, and next action |
| MB-005I | Model execution smoke | As a maintainer, I can verify the local MoveNet model loads and runs inference without a camera | Done | `npm run model:movenet:smoke` executes TensorFlow.js MoveNet SinglePose Lightning against a synthetic local frame |
| MB-005J | Model readiness report | As a maintainer, I can keep local model load and inference performance as release evidence | Done | `npm run model:movenet:readiness` writes a budgeted JSON report and the launch doctor detects it |
| MB-005K | MoveNet pose contract | As a maintainer, I can prove MoveNet-shaped keypoints map into the app analysis contract | Done | A reusable mapper converts MoveNet keypoints to `PoseFrame` and tests replay mapped frames through the analyzer |
| MB-005L | Model-analysis replay report | As a maintainer, I can prove model-shaped keypoints produce privacy-safe metrics and cues across bundled attempts | Done | `npm run model:analysis:replay` writes a durable report for slab, vertical, and overhang attempts and is included in the release gate |
| MB-006 | Coach review | As a coach, I can review athlete reports with consent | Done | Local coach review packet with consent metadata; multi-user workspace future |
| MB-006A | Coach context | As a coach, I can see privacy-safe athlete practice context in a consented packet | Done | Packet includes training-log scores, cue feedback ratings, and drill-practice counts while excluding private note text and drill notes |
| MB-006B | Coach library | As a coach, I can see a local queue of consented review packets | Done | Sessions shows a privacy-safe coach library from active consent records, review priority, signal status, feedback counts, and practice counts |
| MB-006C | Team templates | As a coach, I can reuse local review templates for classes and projects | Done | Sessions generates privacy-safe team templates from consented library signals without raw video, landmarks, or private notes |
| MB-006D | Library export | As a coach, I can prepare a privacy-safe batch handoff from consented local packets | Done | Sessions exports a versioned coach library bundle with queue metadata, team templates, negative privacy flags, and raw-artifact key rejection |
| MB-006E | Native export share | As a coach or product reviewer, I can share a prepared local export through the device share sheet | Done | Sessions exposes a share action only after a prepared export exists and keeps payload preparation local |
| MB-007 | Dependency hygiene | As a maintainer, I receive dependency update PRs | Done | Dependabot configured for npm and GitHub Actions; `npm ci` and moderate-or-higher audit gate pass with the Expo `xcode`/`uuid` override |
| MB-008 | Native release readiness | As a maintainer, I can create internal test builds | Should | EAS profiles, app identifiers, and standard validator are configured; strict gate needs real Expo, Apple, and Google credentials |
| MB-008A | Launch readiness cockpit | As a maintainer, I can see which launch tracks are ready without digging through release docs | Done | Plan tab summarizes stakeholder demo, internal native beta, and store-submission readiness from configurable evidence |
| MB-008B | Launch readiness doctor | As a maintainer, I can detect when launch evidence flags drift from local artifacts | Done | `npm run release:readiness` writes a JSON report with configured, detected, verified, missing, and drift statuses |
| MB-008B1 | Release gate report | As a release owner, I can prove the local release gate from machine-readable step results | Done | `npm run release:check` writes `docs/sdlc/release-gate-report.json` with ordered pass/fail step evidence |
| MB-008B2 | Launch readiness content validation | As a release owner, I can trust machine-detected evidence only after its validators pass | Done | Launch doctor validates native QA evidence and cue-validation datasets before marking those checks verified |
| MB-008B3 | Model replay readiness validation | As a release owner, I can see model-shaped cue evidence as a first-class launch check | Done | Launch doctor validates `docs/sdlc/model-analysis-replay-report.json` before demo, beta, or store tracks can be ready |
| MB-008C | Native QA runbook | As a QA lead, I can generate a physical-device runbook before collecting native evidence | Done | `npm run native:qa:runbook` writes platform workflows, budgets, privacy instructions, and a draft evidence payload that remains blocked until real values are entered |
| MB-008D | Native QA placeholder rejection | As a release owner, I can prevent template placeholders from satisfying device QA | Done | Native QA validation rejects placeholder device identities, build ids, and clip ids even when workflows are marked pass |
| MB-008E | Native QA evidence kit | As a QA lead, I can see native evidence requirements inside the app before beta or store submission | Done | Plan tab shows Android/iOS physical-device runs, workflow count, latency/battery/thermal budgets, placeholder policy, and the validator command |
| MB-008F | Native QA validator preview | As a QA lead, I can preview native evidence readiness inside the app before running the CLI gate | Done | Plan tab shows ready runs, blocking checks, raw-artifact exclusion, and app/CLI parity coverage for native evidence validation |
| MB-008G | Evidence collection plan | As a product lead, I can see exactly which real-world evidence must be collected before production claims | Done | Plan tab derives clip targets, review rows, balanced wall-angle collection batches, native workflow checks, privacy checklist, and external owners from validation contracts |
| MB-008H | Release unblock checklist | As a release owner, I can see the exact external proof, commands, and secret key names needed to unblock beta or store release | Done | Plan tab derives external blocker labels/actions from launch readiness and lists proof artifacts, commands, owners, affected tracks, and credential key names without secret values |
| MB-008I | Release handoff packet | As a release owner, I can hand buyers or stakeholders one packet with current proof, blockers, screenshots, repo commit, and verification commands | Done | `npm run release:handoff` writes JSON and Markdown packets from machine-readable reports, store manifest screenshots, and launch-readiness blockers |
| MB-008J | Release archive integrity | As a release owner, I can verify delivered zip archives before handoff | Done | `npm run release:archives` writes source/web zip archives plus SHA-256 JSON and Markdown manifests in the output directory |
| MB-008K | GitHub Actions CI template | As a maintainer, I can activate regression-blocking CI when workflow-scope credentials are available | Done | `docs/sdlc/ci-templates/github-actions-quality.yml` runs `npm run ci` from lockfile-installed dependencies on pushes to `main` and pull requests, then uploads release evidence artifacts after activation |
| MB-008L | Native QA evidence import | As a release owner, I can paste physical-device QA evidence in the app before committing it | Done | Plan tab parses native QA evidence JSON locally, reports ready runs, blocking checks, invalid JSON, and raw-artifact rejection without cloud upload |
| MB-008M | iOS toolchain doctor | As a release owner, I can see whether this machine can run iOS builds before beta or store work | Done | `npm run native:ios:doctor` writes JSON/Markdown evidence for selected Developer directory, full Xcode, workspace, Pods, and build-settings readiness |
| MB-008N | Store credentials doctor | As a release owner, I can see which EAS, App Store, and Play credential keys are missing without exposing values | Done | `npm run release:credentials:doctor` writes JSON/Markdown evidence for project id, Expo token, iOS submit keys, and Android submit keys with values excluded |
| MB-008O | Native QA evidence composer | As a QA lead, I can compose physical-device evidence from structured real run measurements before committing JSON | Done | Plan tab composes Android/iOS run evidence locally, normalizes seconds to milliseconds, reuses the native QA validator, and keeps raw artifact references blocked |
| MB-008P | Cue validation dataset doctor | As a product lead, I can see why the real-review cue dataset is missing or blocked without copying reviewer identities | Done | `npm run validation:cue:doctor` writes JSON/Markdown evidence for missing, malformed, ready, or failed cue-validation datasets without embedding dataset rows |
| MB-008Q | Native QA evidence export | As a QA lead, I can prepare a share-safe native QA evidence JSON from structured run measurements | Done | Plan tab prepares a versioned native QA evidence composer export with validator summary and rejects raw local paths, video URIs, credential values, and token-like strings |
| MB-009 | Privacy-safe observability | As a maintainer, I can inspect diagnostics without leaking sensitive data | Done | Redaction utilities, aggregate support packet, Privacy UI, and smoke coverage |
| MB-010 | Screenshot documentation | As a stakeholder, I can inspect generated product screenshots from the docs | Done | `docs/screenshots.md` references generated screenshots from `npm run store:screenshots` |
| MB-011 | Restore preview | As a climber, I can validate a local backup before writing it into storage | Done | Privacy tab previews restore, new, existing, skipped orphan, and integrity-check counts with the same schema/privacy validation as restore |
| MB-012 | Backup integrity manifest | As a privacy-conscious climber, I can detect edited local backup payloads before restore | Done | Local backups include a deterministic offline checksum, preview and restore receipt show checksum status, legacy checksum-less backups remain readable, and tampered checksum-bearing payloads are rejected |
| MB-013 | Pre-send guard | As a climber, I can decide whether to reset, repeat, or try hard from local evidence | Done | Progress tab combines analysis quality, readiness, fix cues, practice follow-through, repeat outcomes, and configurable thresholds into a tested local guard |
| MB-014 | Beta memory | As a climber, I can reuse beta that improved or sent a comparable repeat | Done | Progress tab turns improved/sent repeat outcomes into privacy-safe beta memory entries, top patterns, and reusable cue recommendations |
| MB-015 | Model evidence | As a stakeholder, I can see which model checks passed and which real-world validation is still missing | Done | Plan tab shows configurable MoveNet readiness, model-shaped replay, and real-video validation status without overstating production accuracy |
| MB-016 | Safety language guard | As a release owner, I can catch medical, injury-prevention, route-safety, or guaranteed-outcome copy before handoff | Done | Plan tab scans product and release copy with replaceable rules, ignores negated policy copy, and reports clear/review status |
| MB-017 | Model evidence sync | As a release owner, I can promote fresh model reports into app configuration without hand-editing JSON | Done | `npm run model:evidence:sync` updates Expo `extra.modelEvidence` from MoveNet readiness and model-analysis replay reports while preserving real-world validation targets |
| MB-018 | Real-world validation campaign | As a product lead, I can see whether consented clips, coach reviews, and wall-angle coverage are moving toward launch evidence | Done | Sessions composes existing cue-validation contracts into local campaign progress, dataset gate status, next action, and privacy-safe status export |
| MB-019 | Validation-aware cue trust | As a coach, I can see when cue trust reflects completed real-review campaign evidence | Done | Coach review packets pass privacy-safe validation evidence into cue trust, downgrade failed/unreviewed cues, and exclude reviewer identities |
| MB-020 | Prepared export file sharing | As a coach or reviewer, I can share prepared exports as local files instead of long message bodies | Done | Sessions writes prepared `.json`, `.csv`, or `.txt` exports to app cache for native file sharing and falls back to text sharing when unavailable |
| MB-021 | Analysis evidence timeline | As a release owner, I can inspect the local analysis path inside each report | Done | Reports include versioned input, pose-provider, quality, cue-generation, performance, and privacy evidence steps with privacy-safe blocking/review statuses |
| MB-022 | Analysis evidence export | As a QA reviewer, I can share only the report evidence timeline | Done | Sessions prepares a versioned evidence-only JSON export with negative privacy flags and URI/path/secret rejection |
| MB-023 | Release unblock packet | As a release owner, I can share the remaining blocker checklist without secrets | Done | Plan prepares a versioned release unblock packet with commands, proof expectations, owners, env key names, negative privacy flags, and token/path rejection |
| MB-024 | Native QA runbook packet | As a QA lead, I can share the physical-device runbook from the app | Done | Plan prepares a versioned native QA runbook packet with physical-device workflows, budgets, blocked draft evidence, validator command, negative privacy flags, and token/path rejection |
| MB-025 | Provider readiness cockpit | As a release owner, I can see which pose providers are active, fallback-safe, native-ready, or blocked | Done | Plan derives primary, fallback, native-target, runtime proof, and privacy-boundary checks from provider capability metadata and app configuration |
| MB-026 | Release evidence packet | As a release owner, I can send one share-safe packet for QA, product validation, and release owners | Done | Plan prepares a versioned JSON packet with launch readiness, model evidence, provider readiness, native QA runbook, blocker checklist, artifact refs, commands, and negative privacy flags |
| MB-027 | Store submission packet | As a release owner, I can hand off store metadata and submission checks without secrets | Done | `npm run store:submission` and Plan prepare a versioned packet with listing metadata, privacy declarations, screenshots, copy-risk scan, commands, negative privacy flags, and token/path rejection |
| MB-028 | GitHub workflow doctor | As a maintainer, I can see whether the GitHub Actions quality workflow can be activated before attempting a push | Done | `npm run release:github:doctor` writes JSON/Markdown evidence for template presence, active workflow status, template parity, GitHub CLI availability, OAuth scopes, and token-value exclusion |
| MB-029 | Dependency license inventory | As a release owner, I can review third-party license obligations before selling or distributing the app | Done | `npm run security:licenses` writes JSON/Markdown evidence from lockfile-installed packages, handles dual permissive licenses, marks local linked modules internal, and reports review/blocker counts without token or absolute path values |
| MB-030 | Validation collection packet | As a product lead, I can share the real-world validation collection plan without raw artifacts | Done | Plan prepares a versioned packet with balanced clip batches, reviewer slot templates, collection commands, negative privacy flags, and raw path/token rejection |
| MB-031 | Commercial readiness cockpit | As a founder, I can see what remains before connecting subscriptions without coupling payments to movement analysis | Done | Plan derives adapter, paid plan mapping, receipt-validation, sandbox, entitlement-boundary, and config-hygiene checks from replaceable billing config and shared plan keys |
| MB-032 | Commercial readiness packet | As a founder, I can share monetization readiness without exposing provider secrets or receipt data | Done | Plan prepares a versioned packet with owner actions, billing readiness summary, negative privacy flags, and token/path/payment-data rejection |
| MB-033 | Cue-validation reviewer onboarding | As a product lead, I can brief coach reviewers without sending raw video or inventing reviewer data | Done | Sessions prepares a versioned reviewer onboarding packet with instructions, review criteria, command checklist, collection summary, negative privacy flags, and raw path/token rejection |
| MB-034 | Real-world model evidence promotion | As a release owner, I can promote model validation only after the real cue-validation dataset doctor is ready | Done | `npm run model:evidence:sync` reads the cue-validation dataset report, promotes real-world validation only when ready and share-safe, and preserves existing targets otherwise |
| MB-035 | Store credentials setup packet | As a release owner, I can brief CI/EAS/App Store/Play setup without leaking credential values | Done | Plan prepares a versioned packet with EAS project binding, Expo token, App Store Connect, and Google Play key names, release commands, readiness summary, negative privacy flags, and token/path rejection |
| MB-036 | Feature completion audit | As a maintainer, I can tell whether tracked work is complete or only blocked by external evidence | Done | `npm run feature:doctor` writes JSON/Markdown evidence from task plan, backlog, traceability, and launch readiness, reports internal gaps separately from external blockers, and is included in `npm run release:check` |
| MB-037 | Validation pilot kit | As a product lead, I can run the first real-world validation pilot without leaking raw video or inventing reviewer scores | Done | Plan prepares a versioned pilot kit with consent principles, wall-angle capture sprints, coach review rules, closeout commands, negative privacy flags, and raw artifact/path/token rejection |
| MB-038 | Environment template doctor | As a maintainer, I can verify setup keys are documented without committing secrets | Done | `npm run release:env:doctor` writes JSON/Markdown evidence for runtime, smoke, and release credential key names in `.env.example`, with credential values required to stay empty |
| MB-039 | Field validation ops packet | As a product and QA lead, I can coordinate real clips, coach reviews, device QA, and release promotion from one packet | Done | Plan prepares a versioned field validation ops packet with ordered phases, owners, artifacts, commands, target clips, review rows, device runs, negative privacy flags, and raw artifact/path/token rejection |
| MB-040 | Release blocker issue packet | As a release owner, I can file GitHub issues for every external release blocker without leaking secrets | Done | Plan prepares a versioned release blocker issue packet plus repository issue template with titles, labels, owners, tracks, proof, commands, env key names, negative privacy flags, and token/path/raw artifact rejection |
| MB-041 | Capture prep protocol | As a climber, I can follow an evidence-based local pre-recording protocol before trusting a video analysis | Done | Analyze shows setup, warm-up, recording, verification, privacy, and retake criteria derived from capture calibration, latest local report quality, weakest metric, and primary cue evidence |
| MB-042 | Smart repeat matching | As a climber, I can compare my latest attempt against the most comparable local baseline, not just the previous clip | Done | Progress uses local smart matching across wall angle, gym, grade, title, cue overlap, recency, tags, and project status, then shows match confidence and reasons without exposing private notes |
| MB-043 | Advanced drill packs | As a climber, I can turn local cues, feedback, and practice follow-through into a structured Pro drill pack | Done | Drills generates a versioned local advanced pack with block progression, intensity, success criteria, readiness score, wall-angle and grade context, and privacy flags that exclude raw video and private notes |
| MB-044 | Configurable coach lenses | As a climber, I can tune the on-device analysis toward footwork, body position, power conservation, or a balanced review | Done | Analyze exposes coach lenses; the local analyzer applies typed threshold and cue-priority overrides, and reports, beta replay, drills, evidence exports, and coach packets retain privacy-safe lens metadata |
